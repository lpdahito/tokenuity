Docker:

docker build -f apps/explorer/Dockerfile -t tokenuity-explorer .
docker compose up
docker compose down
docker compose rm -f

docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build


docker exec -it mongo-server mongosh